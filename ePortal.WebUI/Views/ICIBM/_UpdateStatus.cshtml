@model ePortal.ViewModels.VM_DGIT_ICAPPHISTORY

@{
    Layout = null;
}
<!doctype html>
<html>
<head>
    <style type="text/css" nonce="@Context.Items["StyleNonce_0"]">
        .loader {
            position: fixed;
            left: 0px;
            top: 0px;
            width: 100%;
            height: 100%;
            z-index: 9999;
            background: url('../../images/loading_double.gif') 50% 50% no-repeat #ffffff;
            opacity: .7;
        }

        .validationError {
            border: 1px solid #a94442;
        }
    </style>
    <script type="text/javascript" nonce="@Context.Items["ScriptNonce_9"]">
        $(document).ready(function () {

        });

        function SaveData() {
            var isFormVaildation = CheckFormValidation();
            if (isFormVaildation) {
                $("#btnSave").attr("disabled", true);
                var data = {
                    ICREQID: $('#hficreqId').val(),
                    APPROVAL_STATUS: $('#ddStatus').val(),
                    APPROVAL_REMARK: $('#txtPrRemark').val(),
                }

                $('#myCreateModel').modal('hide');
                $.ajax({
                    type: "POST",
                    url: "/ICIBM/RejectICRequest",
                    beforeSend: function () {
                        $("#ajaxLoader").addClass('loader');
                    },
                    data: JSON.stringify(data),
                    contentType: "application/json; charset=utf-8",
                    datatype: "json",
                    success: function (data) {
                       
                        if (data == 1) {
                            swal({
                                title: "",
                                text: "Record saved successfully.",
                                type: "success",
                            }, function (isConfirm) {
                                if (isConfirm) {
                                    $("#btnSubmit").click();
                                    //window.location.href = "/PR/PRReport";
                                }
                            });
                        }
                        else if (data == -2) {
                            swal({
                                title: "",
                                text: "Record saved successfully, But mail not sent.",
                                type: "success",
                            }, function (isConfirm) {
                                if (isConfirm) {
                                    $("#btnSubmit").click();
                                }
                            });
                        }
                        else {
                            sweetAlert("", "Record not saved.", "warning");
                        }
                    },
                    complete: function () {
                        $("#ajaxLoader").removeClass('loader');
                    },
                    error: function () {
                        sweetAlert("Oops...", "Something went wrong!", "error");
                    }
                });
            }
        }

        function CheckFormValidation() {
            var isVailed = true;
            $('#ddPrStatus').removeClass('validationError');
            if ($('#ddPrStatus').val() == "") {
                $('#ddPrStatus').addClass('validationError');
                isVailed = false;
            }
            $("#txtPrRemark").removeClass('validationError');
            if ($('#txtPrRemark').val() == "") {
                $("#txtPrRemark").addClass('validationError');
                isVailed = false;
            }
            return isVailed;
        }
    </script>
    <script nonce="@Context.Items["ScriptNonce_8"]">
        $(document).ready(function () {
            $("#btnSave").on('click', function () {
                SaveData();
            });
        });
    </script>

</head>

<body>
    <div class="body-content" style="padding:0px !important;">
        <div>
            <div class="form-horizontal">
                @Html.ValidationSummary(true, "", new { @class = "text-danger" })
                <div class="form-group">
                    @Html.LabelFor(model=>model.ICTitle, htmlAttributes: new { @class = "control-label col-md-3" })
                    <div class="col-md-8">
                        @Html.DisplayFor(model => model.ICTitle, new { @class = "form-control" })
                    </div>
                </div>

                <div class="form-group">
                    @Html.HiddenFor(model => model.ICREQID, htmlAttributes: new { @id = "hficreqId" })
                    @Html.LabelFor(model => model.APPROVAL_STATUS, htmlAttributes: new { @class = "control-label col-md-3" })
                    <div class="col-md-3">
                        @Html.DropDownListFor(model => model.APPROVAL_STATUS, new List<SelectListItem>
                                    {
                                       new SelectListItem{ Text="-Select-", Value = "" },
                                       new SelectListItem{ Text="Reject", Value = "3" },
                                       new SelectListItem{ Text="Send Back", Value = "2" },
                                    }, new { @class = "form-control", @id = "ddStatus" })
                        @Html.ValidationMessageFor(model => model.APPROVAL_STATUS, "", new { @class = "text-danger" })
                    </div>
                </div>
            
                <div class="form-group">
                    @Html.LabelFor(model => model.APPROVAL_REMARK, htmlAttributes: new { @class = "control-label col-md-3" })
                    <div class="col-md-8">
                        @Html.TextAreaFor(model => model.APPROVAL_REMARK, new { @class = "form-control", @id = "txtPrRemark", @style = "max-width:354px; min-height:110px;" })
                        @Html.ValidationMessageFor(model => model.APPROVAL_REMARK, "", new { @class = "text-danger" })
                    </div>
                </div>

                 <div class="form-group">
                    <div class="col-md-offset-4 col-md-8">
                        <input type="button" value="Save" class="btn btn-success" id="btnSave" />
                        <button type="button" class="btn btn-default" data-dismiss="modal" aria-label="Close">Cancel</button>
                    </div>

                </div>
            </div>
        </div>
    </div>

    <div id="ajaxLoader"></div>

</body>
</html>
