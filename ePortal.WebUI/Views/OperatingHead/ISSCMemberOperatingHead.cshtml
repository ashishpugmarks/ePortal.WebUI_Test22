@model ePortal.ViewModels.OperatingHeadAddNominationVM
@{
    ViewBag.Title = "ISSC Member Operating Head";
    Layout = "~/Views/Shared/_Layout.cshtml";
    var opertingmodel = ViewBag.OperatingModel as List<ePortal.ViewModels.OperatingHeadSearchModel1>;
}

@section Styles{
    <link rel="stylesheet" href="~/css/jquery-ui.css">
    <link rel="stylesheet" href="~/lib/bootstrap/css/bootstrap-multiselect.css">
    <link href="~/css/index.css" rel="stylesheet" type="text/css" media="all" />

    <style type="text/css" nonce="@Context.Items["StyleNonce_0"]">
        .btn {
            background-color: #e3e3e3;
            color: #333;
            border: 1px solid #e6e6e6;
            border-radius: 3px;
            padding: 6px 12px;
            font-size: 16px;
            width: 329px !important;
        }

        .bttn {
            background-color: #e3e3e3;
            color: #333;
            border: 1px solid #e6e6e6;
            border-radius: 3px;
            padding: 6px 12px;
            font-size: 16px;
        }

        .dropdownt {
            width: 326px !important;
        }

        .resr {
            font-weight: bold;
            background-color: #C3E214;
            border-radius: 5px;
            border: 2px solid #ccc;
            text-align: center;
            padding: 5px 15px;
            width: 120px;
            font-size: 12px;
        }

        .CS {
            background-color: #abcdef;
            color: Yellow;
            border: 1px solid #AB00CC;
            font: Verdana 10px;
            padding: 1px 4px;
            font-family: Roboto-Regular;
        }

        * {
            box-sizing: border-box;
        }

        .dropdown-check-list {
            display: inline-block;
        }

            .dropdown-check-list .anchor {
                position: relative;
                cursor: pointer;
                display: inline-block;
                padding: 5px 50px 5px 10px;
                border: 1px solid #ccc;
            }

                .dropdown-check-list .anchor:after {
                    position: absolute;
                    content: "";
                    border-left: 2px solid black;
                    border-top: 2px solid black;
                    padding: 5px;
                    right: 10px;
                    top: 20%;
                    -moz-transform: rotate(-135deg);
                    -ms-transform: rotate(-135deg);
                    -o-transform: rotate(-135deg);
                    -webkit-transform: rotate(-135deg);
                    transform: rotate(-135deg);
                }

                .dropdown-check-list .anchor:active:after {
                    right: 8px;
                    top: 21%;
                }

        dropdown-check-list ul.items {
            padding: 2px;
            display: none;
            margin: 0;
            border: 1px solid #ccc;
            border-top: none;
            width: 370px !important;
        }

        .dropdown-check-list ul.items li {
            list-style: none;
        }

        .dropdown-check-list.visible .anchor {
            color: #0094ff;
        }

        .dropdown-check-list.visible .items {
            display: block;
        }

        .cellpadding {
            padding: 5px 5px;
        }

        .form-wrapper {
            margin-top: 20px;
        }

        .spret {
            border: 1px solid #ccc;
        }

        .form-label {
            color: #000;
            font-size: 10px;
        }

        .form-input {
            font-size: 13px;
            padding: 2px 5px;
            border: 1px solid #ccc;
            // width:220px !important;
        }

        .field-indicator {
            color: #F00;
            vertical-align: middle;
        }

        .multiselect-container > li > a {
            padding: 0px;
            width: 320px !important;
        }

        table.dataTable thead th, table.dataTable thead td {
            padding: 8px 12px;
        }

        .table > tbody > tr > td, .table > tbody > tr > th, .table > tfoot > tr > td, .table > tfoot > tr > th, .table > thead > tr > td, .table > thead > tr > th {
            line-height: 1.0;
            vertical-align: middle;
            font-family: "Helvetica Neue", Helvetica, Arial;
        }

        table.dataTable tbody td {
            padding: 4px 3px;
        }

        .table > tbody > tr:nth-of-type(odd) {
            background: #e9e9e9;
        }

        .bttn {
            background-color: #e3e3e3;
            color: #333;
            border: 1px solid #e6e6e6;
            border-radius: 3px;
            padding: 6px 12px;
            font-size: 16px;
        }

        .ui-widget-header, .ui-state-default, ui-button {
            font-weight: bold;
            font-size: 16px;
        }

        .ui-dialog .ui-dialog-content {
            font-size: 14px;
        }

        .ui-dialog-buttonset {
            font-size: 16px;
        }

        .btn1 {
            width: 190px !important;
            font-size: 14px !important;
            background-color: #e3e3e3;
            border: 1px solid #e6e6e6;
            border-radius: 3px;
            padding: 6px 12px;
        }

        .ui-dialog-titlebar-close:before {
            content: "x";
        }
    </style>
}

<div class="row">
    <div style="height: 2.2em; font-size: large; font-weight: bold;font-family: Roboto-Regular; background-color: #8A8A8A; color: #FFF; vertical-align: middle;width:98%;margin-left:1%;">
        <div style="padding:8px;">@TempData["PageHead"]</div>
    </div>
    @if (TempData["msg"] != null)
    {
        <span class="error">
            @TempData["msg"].ToString()
        </span>
    }
    @*@if (Model.SaveStatus > 0)
        {
            <span class="error">
                @Model.SaveMsg
            </span>
        }*@
    @using (Html.BeginForm("ISSCMemberOperatingHead", "OperatingHead", FormMethod.Post, new { encType = "multipart/form-data" }))
    {
        @*@Html.AntiForgeryToken()
                @Html.ValidationSummary("", new { @class = "text-danger" })*@
        // @Html.HiddenFor(x => x.syki)
        @Html.Hidden("statuscheck", (object)ViewBag.Check)
        @Html.Hidden("Periodcheck", (object)ViewBag.PeriodCheck)
        <div class="body-content" style="overflow-x:hidden">
            <div id="msform">
                <table class="table table-bordered table-hover table-responsive" id="mstable">
                    <tbody>
                        <tr>
                            <td class="text-right">
                                <b>ki<span style="color: #ff0000">*</span></b>
                            </td>
                            <td class="col-md-3">
                                <div class="col-md-12">
                                    @Html.DropDownList("SYKIID", ViewBag.SYKIData as SelectList, new { @class = "form-control", @style = "width:100px;" })
                                </div>
                            </td>
                            <td class="col-md-3">&nbsp;</td>
                            <td class="col-md-3"> &nbsp;</td>
                            <td class="col-md-3">&nbsp;</td>
                        </tr>
                    </tbody>
                </table>
                <table class="table table-bordered table-hover table-responsive">
                    <thead>
                        <tr style="background-color:rgba(44, 169, 226, 1);">
                            <th style="text-align:center;width:2% !important;">S.No.</th>
                            <th style="width:30%">Division</th>
                            <th style="width:30%">9</th>
                            <th style="width:30%">9r</th>
                        </tr>
                    </thead>
                    <tbody>

                        @if (opertingmodel != null && opertingmodel.Count > 0)
                        {
                            int index = 1;
                            foreach (var item in opertingmodel)
                            {
                                <tr>
                                    <td style="text-align:center">@index</td>
                                    <td>
                                        @item.DIVISION
                                        @Html.Hidden("DIVISIONIDs", item.DIVISIONID)
                                    </td>
                                    <td>@Html.DropDownListFor(model => model.ISSCMEMBERs, new SelectList(item.ISSCMEMBER, "empcode", "Empname", item.Current_Ki_Empcode), new { @class = "form-control s" })</td>
                                    <td>@item.OLDISSCMEMBER</td>
                                </tr>
                                index++;
                            }
                        }
                        @*@if ((bool)TempData["periodsettingdate"] == true)
                                {*@
                        <tr>
                            <td colspan="6" style="text-align:center;">
                                <div class="col-md-12" style="align-items: center;">
                                    @Html.HiddenFor(model => model.Status)
                                    @*@((bool)TempData["periodsettingdate"] == false ? "disabled='disabled'" : ""*@
                                    <input type="submit" value="Save as Draft" class="bttn btn-success btn_disable" name="action" id="btnSaveAsDraft" )
                                           style="background-color: #ccc; border-color: #4e4e4e; align-items: center;width: 10%;color: #232121;font-weight : bold ;" />
                                    @*@((bool)TempData["periodsettingdate"] == false ? "disabled='disabled'" : ""*@
                                    <input type="submit" class="bttn btn-success btn_disable" value="Submit" id="btnSubmit" )
                                           name="action" style="background-color: #ccc; border-color: #4e4e4e; width: 10%; color: #232121; font-weight : bold ;" />

                                </div>
                            </td>
                        </tr>
                        @*}*@
                        <tr>
                            <td colspan="9">
                                <a title="Back" href="/TokenBridge/RedirectToOldApp?target=/Home/Home">
                                    <img src="~/images/icon_back.gif" border="0" alt="">
                                    Back
                                </a>
                            </td>
                    </tbody>
                </table>
            </div>
        </div>
    }
</div>
<div class="form-group">
    <table e class="table table-bordered table-hover table-responsive">
        <tr>
            <td style="background-color:antiquewhite;font-size:18px;">
                <b>Note:</b>
            </td>
        </tr>
        <tr>
            <td style="background-color:#e3e3e3;">
                <a style="color:black; padding-top: 20px;font-style: initial;font-size:15px;"><b>➢</b></a>
                <a href="~/Asset Register/ISSC Nomination File/Roles & Responsibility.pdf" target="_blank" style="height:30px; font-size:15px;background-color: none;  color: red;border:none;padding-left: 0px; border-radius: 0px;width: 70px !important">
                    Click here
                </a>
                <a style="color:black; padding-top: 20px;padding-left: 0px;font-size:15px;">to check ISSC Member’s Roles & Responsibility.</a>
            </td>
        </tr>
        <tr>
            <td colspan="6">
                <p style="color:black; padding-top: 20px;font-size:15px;">
                    ➢   Department Head and above are eligible for ISSC Member. In case <b>Department Head</b> is not available,<b>Assistant Manager</b> can be nominated exceptionally.
                </p>
            </td>
        </tr>
    </table>
</div>

@section Scripts {
    <script type="text/javascript" language="Javascript" src="~/js/jquery-ui.js"></script>
    <script type="text/javascript" language="Javascript" src="~/js/jquery-ui.min.js"></script>
    <script type="text/javascript" src="~/lib/bootstrap/js/bootstrap.min.js"></script>
    <script type="text/javascript" src="~/lib/bootstrap/js/bootstrap-multiselect.js"></script>


    <script type="text/javascript" nonce="@Context.Items["ScriptNonce_3"]">
         $(document).ready(function () {
         debugger
            var data = $("#statuscheck").val();
            var periodcheck = $("#Periodcheck").val();
            if (periodcheck == 0) {
                if (data > 0) {
                     $('#btnSubmit').hide();
                     $('#btnSaveAsDraft').hide();
                     $('.s').prop("disabled", true);
                }
            }
            else {
                $('#btnSubmit').hide();
                $('#btnSaveAsDraft').hide();
                $('.s').prop("disabled", true);
            }

             var value = $("#SYKIID option:selected").text();
                value = parseInt(value);
                var Other = value;
                var Otherr = value - 1;
                $("table tr>th:nth-child(3)").html(Other + "ki ISSC Member");
                $("table tr>th:nth-child(4)").html(Otherr + "ki ISSC Member");
        });

         $(function () {
            $('#StartDate').datepicker({
                format: "dd/mm/yyyy",
                todayHighlight: true,
                autoclose: true,
                clearBtn: true,
            });

            $('#EndDate').datepicker({
                format: "dd/mm/yyyy",
                todayHighlight: true,
                autoclose: true,
                clearBtn: true,
            });
            $("#btnSaveAsDraft").on("click", function () {
                $("#Status").val(0);
            });
            $("#btnSubmit").on("click", function () {
                $("#Status").val(1);
            });
        });

         function BtnBack(ths, event) {
            window.location.href = "Search";
        }

          $("#BtnCancel").click(function () {
            location.reload(true);
        });

        $("#btnSubmit1").click(function () {
            //Loop through the Table rows and build a JSON array.
            var customers = new Array();
            var isLoop = false;
            $("#mstable TBODY TR").each(function () {
                if (isLoop == true) {
                    var row = $(this);
                    var customer = {};
                    customer.SYKIID = row.find("TD").eq(0).html();
                    var tdObject = row.find('td:eq(2)'); //locate the <td> holding select;
                    var selectObject = tdObject.find("select"); //grab the <select> tag assuming that there will be only single select box within that <td>
                    var selCntry = selectObject.val(); // get the selected country from current <tr>
                    customer.OperationID = selCntry;
                    customers.push(customer);
                }
                isLoop = true;
            });
            var datavalue = JSON.stringify(customers);
            //Send the JSON array to Controller using AJAX.
            $.ajax({
                type: "POST",
                url: "/OperatingHead/InsertCustomers",
                data: { value: datavalue },
                //contentType: "application/json; charset=utf-8",
                dataType: "json",
                success: function (r) {
                    alert(r + " record(s) inserted.");
                }
            });
        });
    </script>    

}

