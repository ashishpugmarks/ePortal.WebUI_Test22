@model IEnumerable<ePortal.ViewModels.AssetDisposalViewModel>

@{
    Layout = null;
}

<style type="text/css" nonce="@Context.Items["StyleNonce_0"]">
    table.dataTable thead th, table.dataTable thead td {
        padding: 8px 12px;
    }

    .table > tbody > tr > td, .table > tbody > tr > th, .table > tfoot > tr > td, .table > tfoot > tr > th, .table > thead > tr > td, .table > thead > tr > th {
        line-height: 1.0;
        vertical-align: middle;
        font-family: "Helvetica Neue", Helvetica, Arial;
    }

    table.dataTable tbody td {
        padding: 4px 3px;
    }

    .table > tbody > tr:nth-of-type(odd) {
        background: #e9e9e9;
    }

    select.input-sm {
        line-height: 10px !important;
    }

</style>

<script type="text/javascript" nonce="@Context.Items["ScriptNonce_4"]">
        $(document).ready(function () {
            $('.homeContent').removeClass('homeContent');
            $("#tbRequestReport").dataTable({
                "bFilter": false,
                "bLengthChange": false
            });
            $(document).on('click', '#btnPrint', function () {
                GetPrint($(this)[0]);
            });
            $(document).on('click', '#btnDetail', function () {
                DetailRequest($(this)[0]);
            });
        });
</script>

<div class="panel-group">
    <div class="panel panel-primary">
        <div class="panel-heading">
            <h2 class="panel-title">
                <strong>Asset Request List</strong>
            </h2>
        </div>
        <div class="panel-body">
            <table class="table table-bordered table-hover table-responsive" id="tbRequestReport">
                <thead>
                    <tr>
                        <th class="text-center">
                            @Html.DisplayNameFor(model => model.DISPOSALHEADERID)
                        </th>
                        <th class="text-center">
                            @Html.DisplayNameFor(model => model.ASSETTYPE)
                        </th>
                        <th class="text-center">
                            @Html.DisplayNameFor(model => model.EMP_NAME)
                        </th>
                        <th class="text-center">
                            @Html.DisplayNameFor(model => model.DATEADDED)
                        </th>
                        <th class="text-center">
                            @Html.DisplayNameFor(model => model.AssetCount)
                        </th>
                        <th class="text-center">
                            @Html.DisplayNameFor(model => model.PROCESSSTATUS)
                        </th>
                        <th class="text-center">Action</th>
                    </tr>
                </thead>
                <tbody>
                    @{ int rowNo = 0; }
                    @if (Model != null)
                                {
                                    foreach (var item in Model)
                                    {
                                <tr>
                                    <td class="text-center" style="width:3%;">
                                        @(rowNo += 1)
                                        @Html.HiddenFor(modelItem => item.DISPOSALHEADERID)
                                    </td>
                                    <td class="text-center">
                                        @Html.DisplayFor(modelItem => item.ASSETTYPE)
                                    </td>
                                    <td class="text-center">
                                        @Html.DisplayFor(modelItem => item.EMP_NAME)
                                    </td>
                                    <td class="text-center">
                                        @Html.DisplayFor(modelItem => item.DATEADDED)
                                    </td>
                                    <td class="text-center">
                                        @Html.DisplayFor(modelItem => item.AssetCount)
                                    </td>
                                    <td class="text-center">
                                        @Html.DisplayFor(modelItem => item.PROCESSSTATUS)
                                    </td>
                                    <td class="text-center">
                                        <button class="btn btn-default btn-xs glyphicon glyphicon-print" title="Click to print" data-id="@item.DISPOSALHEADERID" id="btnPrint"></button>
                                        <button class="btn btn-info btn-xs glyphicon glyphicon-tasks" title="Click to detail" data-id="@item.DISPOSALHEADERID" id="btnDetail"></button>
                                    </td>
                                </tr>
                                    }
                                }
                </tbody>
            </table>
        </div>
    </div>
</div>


